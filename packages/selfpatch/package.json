{
  "name": "@sparkly/selfpatch",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Self-patching controller: detects the running OpenCode binary, patches the exact-version source, rebuilds, and swaps in the patched binary automatically with progress feedback and restart.",
  "main": "index.js",
  "exports": {
    ".": "./index.js",
    "./*": "./*"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
